<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.freeletics.flowredux.SampleApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity android:name="com.freeletics.flowredux.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.freeletics.flowredux.compose.ComposeActivity"
            android:exported="false" />

        <activity
            android:name="com.freeletics.flowredux.traditional.TraditionalPopularRepositoriesActivity"
            android:exported="false" />
    </application>

</manifest>
