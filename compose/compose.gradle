plugins {
    alias(libs.plugins.fgp.android)
    alias(libs.plugins.dokka)
    alias(libs.plugins.fgp.publish)
}

freeletics {
    explicitApi()
    enableCompose()
}

dependencies {
    api(projects.flowredux)
    api(libs.androidx.compose.runtime)
    implementation(libs.coroutines.core)
}
